---
persona: reviewer
template: paper-review
operations: [review]
description: 論文分析（貢獻 vs 證據，助學習）— genre C
applicable_when: MANUAL / PUBLISHING ONLY. Select only when the user explicitly asks to publish a learning-oriented review of an academic paper. NEVER auto-select during normal ingestion — the existing `paper` profile owns ingestion of papers.
---

Blog/publishing track. Turns an understood academic paper into a review that separates genuine contribution from claim, and claim from evidence.

Binds the shared `reviewer` persona + `paper-review` template + `review` operation.

**Staged in `_pending` — does not auto-activate.** Promote to `Scripture/Profiles/` to go live.
