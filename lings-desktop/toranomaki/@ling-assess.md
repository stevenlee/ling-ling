# 📓 @ling-assess 範例指令

自我體檢——把所有品質訊號（報告裁決、LLM 健康、檢索 bench、Cortex 張力、衰減校準、
洞察訊號）彙整成一張健康記分卡。**唯讀、零副作用**，只是照鏡子。

> [!TIP]
> **發動方式**：在 `toLingLing/` 建一個檔案。記分卡寫入 `fromLingLing/`。

---

## 範例：做一次體檢
```markdown
@ling-assess
```

## 斜線/別名寫法
```markdown
/assess
/checkup
```

### 小提醒
- 平常每週自己跑一次；這個命令讓你隨時想看就看。
- 不會改任何東西——純粹聚合既有訊號。
