# 📓 @ling-ledger 範例指令

跑一次 Cortex falsification 帳本：把累積 ≥2 條獨立矛盾的主張標記為已推翻，並追蹤
merge/unmerge 事件以調整裁決嚴格度。平常由夜間排程執行。

> [!TIP]
> **發動方式**：在 `toLingLing/` 建一個檔案。摘要寫入 `fromLingLing/`。

---

## 範例
```markdown
@ling-ledger
```

### 小提醒
- 想看「哪些信念有問題」的人類可讀版，用 `@ling-tensions`；這個命令是底層帳本維護。
