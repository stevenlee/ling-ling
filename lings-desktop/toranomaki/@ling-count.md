# @ling-count

`@ling-count` is a legacy alias for `@ling-lens`.

Prefer:

```markdown
@ling-lens [[Article Name]]
Count: appeals to authority
```

`/count` also remains available as a legacy alias for `/lens`.
