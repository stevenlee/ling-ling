
> [!TIP]
> **發動方式**：將此檔案拖入 `toLingLing/` 資料夾。
> 
> **範例用法**：
> - **自動模式** (不寫任何內容)：玲玲會自動從 `Backups/` 抓取最新的備份進行匯入。
> - **指定模式**：在內容中寫下 `[[kb_backup_2026xxxx.zip]]` 即可指定特定日期。
> 
> **安全機制**：
> - 衝突處理：若檔案已存在且內容不同，會自動更名為 `(twin).md`。
> - 排除機制：系統會自動跳過 `toLingLing/` 裡的指令檔，避免死循環。
